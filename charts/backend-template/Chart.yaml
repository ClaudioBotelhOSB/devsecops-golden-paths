apiVersion: v2
name: backend-template
description: Stack-agnostic backend service chart. Override nameOverride / fullnameOverride per service, set image.repository on install, and supply container.command/args/ports/probes per workload. The chart does not assume language or framework.
type: application
version: 0.4.0
appVersion: "0.4.0"
kubeVersion: ">=1.28.0-0"
keywords:
  - golden-path
  - platform
  - backend
